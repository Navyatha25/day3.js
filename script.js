// script.js

// Find the HTML element with id="message"
let messageElement = document.getElementById("message");

// Change its content
messageElement.innerHTML = "Hello! This message is displayed using an external JavaScript file.";
